import './dropArea.css';

import {useDroppable } from '@dnd-kit/core';

export default function DropArea() {
    const {setNodeRef} = useDroppable({
        
    })
    return (
        <div >
        </div>
    )
}